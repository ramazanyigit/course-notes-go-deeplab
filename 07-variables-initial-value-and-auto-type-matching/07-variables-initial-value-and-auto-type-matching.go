package main

import "fmt"

func main() {
	// Define a variable with initial value and no type.
	// Variable's type will be reflected by the compiler and set automatically when compiling.
	var message = "Hello Go!"

	fmt.Println(message)
}